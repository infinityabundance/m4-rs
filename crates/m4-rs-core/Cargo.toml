[package]
name = "m4-rs-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Core macro expansion engine for m4-rs: lexer, parser, evaluator, builtins."

[dependencies]
regex = "1"
